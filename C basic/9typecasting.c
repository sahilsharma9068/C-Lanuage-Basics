#include <stdio.h>
/* we use typecasting to change the data type like if we have to change float data tyoe into int data type then we use type casting*/
int main()
{    // float b =19; 
    // printf("the value of b is %d ", (int)b );
    int a ; 
    float b = (float)45/7; 
    printf("the value of b is %f" ,   b ); 
    return 0;
    
    
}